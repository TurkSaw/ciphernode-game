# CipherNode Game Environment Variables
# Copy this file to .env and update the values

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration - CRITICAL: Change JWT_SECRET in production!
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-generate-64-byte-hex
JWT_EXPIRES_IN=24h

# Database Configuration - Supabase Only
# Get these values from your Supabase project dashboard
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# Game Configuration
ENERGY_REGENERATION_MINUTES=5
MAX_ENERGY=100
GAME_ENERGY_COST=10
INITIAL_ENERGY=100

# Security Configuration
BCRYPT_ROUNDS=10
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Socket.IO Configuration
# Add your production domain to CORS origins
SOCKET_CORS_ORIGIN=http://localhost:3000,https://yourdomain.com
SOCKET_PING_TIMEOUT=60000
SOCKET_PING_INTERVAL=25000

# CORS Configuration
# Add your production domain to allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://yourdomain.com

# Production Configuration (for deployment)
# Uncomment and set these for production deployment
# NODE_ENV=production
# PORT=3000
# RENDER_EXTERNAL_URL=https://your-app.onrender.com

# =============================================================================
# SUPABASE SETUP INSTRUCTIONS
# =============================================================================
# 
# 1. Create a new project at https://supabase.com
# 2. Go to Project Settings > API
# 3. Copy your Project URL (format: https://xxxxxxxxxxxxx.supabase.co)
# 4. Copy your anon/public key (starts with eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...)
# 5. Go to SQL Editor and run the schema from supabase-schema.sql
# 6. Enable Row Level Security policies (already included in schema)
# 
# Example URLs:
# SUPABASE_URL=https://rrnhspojaiiwrdxmdxfs.supabase.co
# SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSI...
# 
# For detailed setup instructions, see SUPABASE-SETUP.md
# =============================================================================